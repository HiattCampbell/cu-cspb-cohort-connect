services:
  db:
    image: "postgres:16-alpine"
    container_name: db
    environment:
      - POSTGRES_DB=cohort
      - POSTGRES_USER=cohort
      - POSTGRES_PASSWORD=cohort
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
